{
  "name": "opencode-vision-bridge",
  "version": "1.0.0",
  "description": "Automatic vision fallback for text-only OpenCode V2 sessions.",
  "private": false,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@opencode/ai": "2.0.12",
    "@opencode/client": "2.0.12",
    "@opencode/plugin": "2.0.12"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
